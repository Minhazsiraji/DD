# P0 Evidence

Track A only. The P0 implementation is governed by `db/manifest.toml` and the
Rev 4.3.2f architecture fingerprint recorded there.

Run `npm run db:verify:p0 -- <local database URL>` after replaying the manifest.
Track B, destructive gates, migration archival, and production deployment are
outside this evidence package.