// Frozen P0 SECURITY DEFINER execute contract. Do not derive this at runtime.
export const P0_DEFINER_EXECUTE = new Map([
  ["add_encounter_diagnosis(uuid,integer,text)", new Set(["authenticated"])],
  ["add_encounter_investigation(uuid,integer,text)", new Set(["authenticated"])],
  ["add_prescription_item(uuid,integer,jsonb)", new Set(["authenticated"])],
  ["allocate_dd_patient_number()", new Set(["authenticated"])],
  ["allocate_queue_token(uuid,date,uuid)", new Set(["authenticated"])],
  ["call_patient(uuid,text)", new Set(["authenticated"])],
  ["can_read_public_booking_contact(uuid)", new Set(["authenticated"])],
  ["cancel_appointment(uuid,cancellation_reason,text)", new Set(["authenticated"])],
  ["check_in_appointment(uuid)", new Set(["authenticated"])],
  ["clear_queue_priority(uuid)", new Set(["authenticated"])],
  ["consume_anon_rate_limit(text)", new Set([])],
  ["correct_public_booking_contact(uuid,text,text,text,text)", new Set(["authenticated"])],
  ["create_clinical_patient(text,uuid)", new Set(["authenticated"])],
  ["create_health_subject(text,subject_kind,text)", new Set(["authenticated"])],
  ["create_internal_appointment(uuid,uuid,timestampwithtimezone,appointment_mode)", new Set(["authenticated"])],
  ["create_prescription_correction(uuid,text)", new Set(["authenticated"])],
  ["create_prescription_template(text,jsonb)", new Set(["authenticated"])],
  ["create_professional_profile(text,profession)", new Set(["authenticated"])],
  ["create_public_booking(uuid,timestampwithtimezone,text,text,text,text)", new Set(["anon","dd_public_ingress"])],
  ["create_walkin_appointment(uuid,text)", new Set(["authenticated"])],
  ["current_doctor_id()", new Set(["authenticated"])],
  ["current_profile_id()", new Set(["authenticated"])],
  ["doctor_active_at(uuid,uuid)", new Set([])],
  ["emit_anon_audit_event(text,text,text,uuid)", new Set([])],
  ["emit_audit_event(text,text,uuid,uuid)", new Set(["authenticated"])],
  ["emit_p0_appointment_metric()", new Set([])],
  ["emit_p0_credential_metric()", new Set([])],
  ["emit_p0_encounter_metric()", new Set([])],
  ["emit_p0_prescription_metric()", new Set([])],
  ["emit_p0_profile_metric()", new Set([])],
  ["finalize_prescription(uuid,integer,jsonb,text,text)", new Set(["authenticated"])],
  ["finish_consultation(uuid,integer)", new Set(["authenticated"])],
  ["grant_health_subject_access(uuid,uuid,subject_authority,text)", new Set(["authenticated"])],
  ["handover_prescription(uuid)", new Set(["authenticated"])],
  ["has_capability(uuid,capability)", new Set(["authenticated"])],
  ["lock_public_booking_chamber(uuid)", new Set([])],
  ["may_hand_over_prescription(uuid)", new Set([])],
  ["may_manage_appointment(uuid)", new Set([])],
  ["may_read_prescription_asset(text)", new Set(["authenticated"])],
  ["may_write_doctor_asset(text,text)", new Set(["authenticated"])],
  ["may_write_prescription_asset(text)", new Set([])],
  ["open_encounter(uuid,uuid)", new Set(["authenticated"])],
  ["open_encounter_for_appointment(uuid)", new Set(["authenticated"])],
  ["open_prescription(uuid)", new Set(["authenticated"])],
  ["prepare_doctor_asset(text)", new Set(["authenticated"])],
  ["prepare_prescription_signature_asset(uuid)", new Set(["authenticated"])],
  ["prescription_review_bundle(uuid)", new Set(["authenticated"])],
  ["public_booking_status(uuid)", new Set(["anon","dd_public_ingress"])],
  ["public_chamber_availability(uuid,date,date)", new Set(["anon","dd_public_ingress"])],
  ["public_chamber_is_eligible(uuid)", new Set([])],
  ["public_slot_is_open(uuid,timestampwithtimezone,timestampwithtimezone)", new Set([])],
  ["record_public_ingress_failure(text,uuid,uuid)", new Set(["dd_public_ingress"])],
  ["refresh_capability_trigger()", new Set([])],
  ["refresh_metric_rollups_after_contribution()", new Set([])],
  ["refresh_profile_capabilities(uuid)", new Set([])],
  ["register_public_booking_patient(uuid,text,text,text)", new Set(["authenticated"])],
  ["reschedule_appointment(uuid,timestampwithtimezone)", new Set(["authenticated"])],
  ["resolve_public_booking_patient(uuid,uuid)", new Set(["authenticated"])],
  ["revoke_health_subject_access(uuid,text)", new Set(["authenticated"])],
  ["search_public_booking_patient_candidates(uuid,text,text)", new Set(["authenticated"])],
  ["set_appointment_status(uuid,appointment_status,text)", new Set(["authenticated"])],
  ["set_queue_priority(uuid,priority_reason,text)", new Set(["authenticated"])],
  ["skip_patient(uuid,text)", new Set(["authenticated"])],
  ["update_encounter_sections(uuid,integer,text,text,text,text,text,text)", new Set(["authenticated"])],
  ["update_prescription_template(uuid,text,jsonb,boolean)", new Set(["authenticated"])],
  ["update_professional_profile(text,text,text,text,text)", new Set(["authenticated"])],
]);
